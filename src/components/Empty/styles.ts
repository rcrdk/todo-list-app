import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    paddingTop: 48,
    borderTopWidth: 1,
    borderTopColor: '#333',
    alignItems: 'center',
  },

  icon: {
    marginBottom: 16,
    color: '#404040',
  },

  title: {
    fontSize: 15,
    lineHeight: 24,
    color: '#808080',
    textAlign: 'center',
    fontWeight: 700,
  },

  text: {
    fontSize: 15,
    lineHeight: 24,
    color: '#808080',
    textAlign: 'center',
  },
})
